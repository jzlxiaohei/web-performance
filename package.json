{
  "name": "web-performance",
  "version": "0.0.3",
  "description": "collect performance for mobile web (using timing api)",
  "main": "./lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack -p",
    "es5": "babel src --out-dir lib",
    "prepublish": "npm run es5 && npm run build"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "babel-core": "^6.3.15",
    "babel-loader": "^6.2.0",
    "babel-preset-es2015": "^6.3.13",
    "babel-preset-stage-0": "^6.5.0",
    "webpack": "^1.13.0"
  }
}
